doctype html
html
  head
    meta charset="UTF-8" /
    title Page not found.
  body
    main
      blockquote
        | You're lost, but its not too late to turn back.


css:
  body 
  {
    min-height: 100vh;
    box-sizing: border-box;
    margin: 0;
    padding-top: calc(50vh - 6em);
    font: 150%/1.6 Tahoma, Verdana, Segoe, sans-serif;
    color: #222;
  }

  body, main::before 
  {
    background: url("404.jpg") 0 / cover fixed;
  }

  main 
  {
    position: relative;
    margin: 0 auto;
    padding: 1em;
    max-width: 23em;
    background: hsla(0,0%,100%,.25) border-box;
    overflow: hidden;
    border-radius: .3em;
    box-shadow: 0 0 0 1px hsla(0,0%,100%,.3) inset,
                0 .5em 1em rgba(0, 0, 0, 0.6);
  }

  main::before 
  {
    content: '';
    position: absolute;
    top: 0; right: 0; bottom: 0; left: 0;
    margin: -30px;
    z-index: -1;
    -webkit-filter: blur(8px);
    filter: blur(8px);
  }
